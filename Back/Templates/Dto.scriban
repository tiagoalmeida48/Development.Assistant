{{~ # Template para gerar DTOs (Data Transfer Objects) ~}}
using System.Collections.Generic;
using System;

namespace {{ project_name }}.App.Dto{{ namespace }}
{
    public class {{ table_name }}Dto : BaseDto
    {
{{~ for column in columns ~}}
{{~ if column.name != "CREATED" && column.name != "UPDATED" ~}}
        public {{ column.Type }}{{ if column.IsNullable }}?{{ end }} {{ column.Name }} { get; set; }

{{~ end ~}}
{{~ end ~}}
    }
}
